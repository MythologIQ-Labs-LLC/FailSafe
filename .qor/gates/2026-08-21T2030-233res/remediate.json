{
  "pattern": "repeated-veto-signature",
  "proposal_kind": "gate",
  "skill": "qor-plan",
  "session_id": "2026-08-21T2030-233res",
  "addressed_event_ids": [
    "ce7c36d8382c05eb4b152499fcc0a4ba65a9c93b7a16ceda5a1047a83e764242"
  ],
  "proposal_text": "SIGNATURE: a check asserted to prove something without exercising what would falsify it. Seven instances across three VETOed iterations of plan-233-read-ledger-once (ledger #592/#593/#594, addendum #595). The highest-frequency and lowest-cost-to-close instance is V1: plan_grep_lint reported '0 citation(s) truth-checked' and exited 0 against a plan carrying nine Locked Decisions, because its _LD_HEADING_RE only scans regions under a 'locked decision'/'citation inventory' heading and the LDs were inline text. The lint recognized nothing and reported success. Read casually, the ladder looked clean.\n\nCOUNTERMEASURE (built, tested, in-repo): FailSafe/extension/scripts/check-plan-citation-parity.cjs compares the COUNT a truth-checking lint reports against the number of items that should have been checked, and fails on any mismatch, on an unobtainable lint output, and on a lint whose output shape no longer carries a count. Pinned by 11 behavioral cases in src/test/scripts/checkPlanCitationParity.test.cjs, including the origin case (three declared / zero checked must FAIL) and a partial-check case (two of three is not a pass) - the gate is proven to discriminate in the direction it guards, not merely to pass on clean input. Verified live against the real nine-LD plan: declared=9 truth-checked=9.\n\nGENERALIZED RULE, for the portion not mechanically closable from this workspace: an exit code cannot distinguish 'I checked and it holds' from 'I did not recognize anything to check'; a count can. Every completeness claim must name the case that would falsify it AND show that case is reachable by the check as written - reachability is a separate question from assertion strength, and it is where four of the seven instances lived (unreachable via missing options, via fixture content, via a cast that neutralized the type, via a baseline captured from the thing under test). For multi-part claims, each part needs its own falsifying check; a proxy measurement of the easy half is not weak evidence for the other half, it is no evidence, and it reads as coverage.\n\nUPSTREAM GAP, surfaced not filed (cross-workspace boundary): qor/scripts/veto_pattern.py emits severity-3 'repeated_veto_pattern' events, but qor/scripts/remediate_pattern_match.py PATTERN_RULES contains no rule for that event type (it covers aged_high_severity_unremediated, hallucination, regression, gate_override/orchestration_override, capability_shortfall only). Such events can therefore never be classified, never produce a proposal, and never be marked addressed - they accumulate unaddressed forever. Confirmed by running classify() over 7 unaddressed groups including this one: returned []. This is the same signature one layer up, in the governance tooling: a detector that cannot see the thing it exists to catch. It is also why this event had to be hand-authored rather than emitted and classified automatically.",
  "closure_enforcer": "cannot-automate: the generalized falsifiability discipline is enforced at /qor-audit Step 3, whose skill source lives in the upstream qor-logic package and cannot be modified from this consumer workspace without creating install drift. The mechanically closable portion IS automated and shipped here as check-plan-citation-parity.cjs with 11 pinning tests; the residue is authoring judgment plus an upstream PATTERN_RULES gap surfaced to the operator for a cross-repo decision.",
  "ts": "2026-08-22T14:09:02Z"
}
