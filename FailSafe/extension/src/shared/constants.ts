// Canonical URL for the FailSafe Pro product download page.
// Used by the openFailSafeProDownload command, Settings webview, and SettingsRoute.
// If the route changes upstream, fix the website redirect — never scatter URLs.
export const FAILSAFE_PRO_DOWNLOAD_URL = "https://mythologiq.studio/failsafe-pro/download";
