# Governance Index

**Last Reviewed**: 2026-09-04 (GATE + IMPLEMENT — **#233 read-once slice, iteration 5, FX930/FX931** (`plan-233-read-ledger-once.md`, Tier 4 plans glob), ledger #598 (audit PASS, resolving finding B2 per explicit owner decision on PR #433 — a directly-constructed absent-ledger temp workspace replaces the unreachable six-fixture claim, matching this repo's own `WorkspaceArtifactBuilder.test.ts`/`consumer-adapter.test.ts` precedent for that class of state) → #599 (implementation: `WorkspaceArtifactBuilder.build()` collapsed 5→3 reads / 2→1 `parseMetaLedgerEntries` calls via `readMetaLedgerRaw`/`applyVersionFloor`, B197 version-floor semantics preserved on both consumers). Pushed to the existing `fix/233-read-ledger-once` branch, continuing PR #433 — not merged. `npm test` (vscode-test) environment-blocked (proxy denies the VS Code binary download host); 122/122 direct-mocha-run cases green as the disclosed mitigation, `test:node` 264/264, lint/tsc/compile/runner-coverage/FEATURE_INDEX-header-parity all green. Formal `/qor-substantiate` version-bump seal deferred to merge time per this repo's own observed convention (see the #392/.../#408 batch merge-substantiation entry below), not performed here. _Prior: 2026-08-22 (DELIVER - **v6.0.3 PUBLISHED** to VS Code Marketplace + Open VSX (run 32576176353, all five stages green; operator clicked the production gate personally, not API-enacted); tag v6.0.3 at 8fced488 == main HEAD; ledger #596. Shipped 18 merged PRs that had accumulated unreleased on main while package.json sat at 6.0.2 - headline #412, the enforce-default upgrade notice that could never fire, meaning v6.0.0 moved pre-existing installs Observe->Enforce silently. Four late-landing green PRs folded in rather than deferred to a 6.0.4. Release re-dated to 2026-08-22 after source metadata disagreed with the heading. Preflight 9/9 run three times incl. against main post-merge. _Prior: 2026-08-21 (RESEARCH — **#233 residual META_LEDGER consumers**, ledger #591; new `docs/research-brief-233-residual-ledger-consumers-2026-08-21.md` (Tier 5, covered by the `docs/research-brief-*.md` glob). CORRECTS ledger #590: the "five raw consumers" list came from a hand-filtered `grep -l` on the filename string and is falsified in both directions — `GovernancePhaseTracker.ts` has zero imports (pure text fns; ledger named only in a doc comment) and `ConsoleLifecycleService.ts:98` is an fs.watch target, so neither is a consumer; `ConsoleServerHub.ts:79` is a deliberate bounded 4KB tail read (measured 0.4ms vs 24.0ms full — migrating would regress 60x AND reintroduce the false-malformed the `complete` flag suppresses); `SystemStateReader.ts:41` wants document-level `## Chain Status:` absent from `MetaLedgerEntry`; only `MetaLedgerReader.ts` is clean, and it changes Console metrics (heading-keyword `kind` vs `**Phase**` field disagree on #589/#590). Measured the defect nobody listed: FIVE full reads of the 1.75MB ledger per `WorkspaceArtifactBuilder.build()` (8.7MB, 477ms cold) on the `CommitCheckRoute` enforce-blocking path, two of them redundant `readMetaLedgerArtifact` calls, plus an unlisted raw consumer at `readGovernanceState:103`. Slice retargeted from "migrate the rest" to "read the ledger once per snapshot". SHADOW_GENOME event recorded (grep-list-as-finding). Advisory only — no code changed. _Prior: 2026-08-21 (GATE — **batch merge substantiation of the seven open PRs** (#392/#394/#399/#402/#405/#407/#408), ledger #590. Board cleared to 0 open PRs; #391/#393/#398/#404 auto-closed; #233 and #367 correctly remain open on disclosed residuals. #408 (the #404 CI-gate repair) was merged FIRST so the later PRs were verified by the repaired gate — it immediately caught FEATURE_INDEX header drift on #399 and #402 (742→743→744). Merge-readiness verdict ONLY: no /qor-audit was run against these diffs, and six of the seven carry no SESSION SEAL of their own — disclosed, not closed. _Prior: 2026-08-21 (DELIVER — **v6.0.2 PUBLISHED** to VS Code Marketplace + Open VSX (run 32454429880, five stages green; operator clicked the production gate personally after I declined to API-enact it); ledger #588 delivery seal. _Prior: 2026-08-21 (GATE — **v6.0.2 delivery preparation, held at the Review Boundary** + PR #387 merge substantiation (watcher disposal, #385); ledger #587; residuals: commit-author identity violation on main (unfixable without history rewrite), #388 filed. _Prior: 2026-08-21 (GATE — **workspace housekeeping: anchored branch prune 131→22**, `plan-workspace-housekeeping` (Tier 4 plans glob), ledger #586 (3 audit iterations; 2 VETO, one preventing local data loss). New archive artifact `docs/archive/branch-prune-manifest-2026-08-21.md` — per-row anchor provenance and the **resolution target for branch names cited in META_LEDGER that no longer resolve as refs**. _Prior: 2026-08-21 (SUBSTANTIATE — **PR-linkage audit signal quality (FX926, #374)**, `plan-linkage-signal-quality-374` (Tier 4 plans glob), ledger #585 (first-iteration PASS with F1 wording fix; selfPr false-fail bonus pinned). _Prior: 2026-08-21 (SUBSTANTIATE — **Shared corrupt-file guard (FX925, #378)**, `plan-corrupt-file-guard-378` (Tier 4 plans glob), ledger #584 (iter-1 VETO B1 coordinate-rot citation → iter-2 PASS; FX923 retrofit message-identical). _Prior: 2026-08-21 (SUBSTANTIATE — **Console risk CRUD durable-store-only mutations (FX924, #377)**, `plan-risk-routes-durable-store-377` (Tier 4 plans glob), ledger #583 (iter-1 VETO B1/B2 citation defects → iter-2 PASS; behavioral red proven by revert-check). _Prior: 2026-08-21 (SUBSTANTIATE — **Risk register corrupt-store preservation (FX923, #368)**, `plan-risks-corrupt-store-368` (Tier 4 plans glob), ledger #582 (iter-1 VETO F1 route-path coverage → writeRisks-level design → iter-2 PASS); collateral issues #377 (route F-6 sibling) + #378 (swallow-overwrite siblings) filed. _Prior: 2026-08-21 (SUBSTANTIATE — **Audit Log CSV export repair (FX922, #370)**, `plan-audit-csv-export-370` (Tier 4 plans glob), ledger #581 (conditional-PASS audit; F1 artifactPath aliasing folded in). _Prior: 2026-08-21 (GATE TRIBUNAL — merge-substantiation records #579 (PR #371 MetaLedgerReader memoize, #244 program) and #580 (PR #366 linkage-audit fail path, #241 F-7); follow-up #374 filed; commit-author identity deviation in the external-relay series surfaced. _Prior: 2026-08-21 (SUBSTANTIATE — **Audit Log verdict signal: enriched sentinel records + severity triage filter (FX921)**, `plan-audit-log-verdict-signal` (Tier 4 plans glob), ledger #578 (audit iter-1 VETO B1 → iter-2 PASS → impl → seal); operator ruling ui-data-point-value-test applied; #367/#370 filed. _Prior: 2026-08-21 (SUBSTANTIATE — **Modal wrapper CSS port + shared selector escaping (#360)**, `plan-modal-css-escape-360` (Tier 4 plans glob), ledger #576, delivered PR #362; plus GATE TRIBUNAL merge-substantiation records #575 (PR #358 notifier envelope) and #577 (PR #364 RiskRegister backlog materialization, audit #241 F-6); cross-agent pointer files `codex.md` + `.github/copilot-instructions.md` registered in Tier 2. _Prior: 2026-08-21 (SUBSTANTIATE — **#242 slice 2: keyboard-operable modal triggers (FX920)**, `plan-242-slice2-modal-triggers` (Tier 4 plans glob), session gates `.qor/gates/2026-08-20T1929-e7f249/`, ledger #570 (VETO) → #571 (PASS) → #572 (impl) → #573 (seal). Upstream Qor-logic#359 filed (secret-scanner allowlist surface). _Prior: 2026-08-21 (SUBSTANTIATE — **Hardening bundle #346/#349/#350 (FX918/FX919)**, `plan-hardening-346-349-350` (Tier 4 plans glob), session gates `.qor/gates/2026-08-20T1820-63e94c/`, ledger #565 (VETO) → #566 (PASS) → #567 (impl) → #568 (seal); branch also carries deviation record #564 (production-gate incident, operator-RATIFIED) and merge-substantiation records #562-#563 context. Delivered via PR under the CI-green+substantiated merge delegation. _Prior: 2026-08-20 (SUBSTANTIATE — **Monitor a11y slice + release-gate What's-New preflight (FX917)**, `plan-monitor-a11y-release-gate` (Tier 4 plans glob), session gates `.qor/gates/2026-08-20T1440-d69349/`, ledger #554 (VETO) → #555 (PASS) → #556 (amendment PASS) → #557 (impl) → #558 (seal). Held at Review Boundary on `fix/monitor-a11y-release-gate`. _Prior: 2026-08-20 (SUBSTANTIATE — **Monitor sentinel-alert Console deep link (FX916)**, `plan-monitor-alert-console-deeplink` (mirrored at `.failsafe/governance/plans/`, Tier 4 plans glob), session gates at `.qor/gates/2026-08-20T0244-4cfa93/`. Full governed cycle incl. an Option-B independent-reviewer VETO (#549) closed at iter-2 PASS (#550), implementation (#551), SESSION SEAL (#552). Deep-link identity = `SentinelVerdict.timestamp` propagated checkpoint+transparency; webview relay `failsafe.openConsole` → `failsafe.openConsoleRoute`. Held local at the Review Boundary; no publish-state change. _Prior: 2026-08-19 (IMPLEMENT — **qor-logic 0.155.0 alignment + enforce-by-default flip** (`plan-qor155-align-enforce-default`, session gates at `.qor/gates/2026-08-19-qor155-align-enforce-default/`): skill payload synced to 0.155.0 (4 tracked files; install_drift_check clean); operator-approved META_LEDGER physical reorder of the #442–#457 band (content-preserving, per-entry SHA256 manifest identical; heals the 3 verifier BREAKs; backup in `.failsafe/archive/`); governance-mode default flipped observe→enforce across 9 resolution sites with observe-alert removal (FX507 removed; FX899–FX903 registered), editor-enforce made tier-independent (operator ruling), enforce-mode createIntent funnel fixed, one-time upgrade notice added, `/api/v1/status` governance/sentinel conflation fixed; FailSafe Pro references removed from both READMEs (operator directive — partial backtrack of the v5 reveal). Audit: iter-1 VETO → operator rulings → iter-2/3 amendments → PASS at META_LEDGER Entry #508. Held local on `feat/qor155-align-enforce-default` at the Review Boundary. _Prior: 2026-07-13 (IMPLEMENT — consumer-stabilization VETO remediation complete; current Tier-4 plan `docs/plan-qor-phase232-consumer-stabilization-veto-remediation.md`, audit iteration 6 PASS at META_LEDGER Entry #496, implementation evidence at Entry #497. Compile, lint, audited 38-file Razor, 3,688-test extension census, 182-test Playwright suite, docs validation, intent lock, and ledger verification are green. Architecture baseline refreshed; held local at the Review Boundary pending authorization for version bump and seal commit.))
)))))))))))

**Last Reviewed**: 2026-08-23 (IMPLEMENT — **Workspace Taxonomy editor → governed tracker-config (FX891)**, held at Review Boundary. Registered two new artifacts: `docs/roadmap/tracker-config.yaml` (Tier 1 — operator-declared programs ∥ verticals + agent mappings, the source of truth cycles consult) + `.failsafe/governance/tracker-taxonomy.directive.md` (Tier 2 — emitted advisory directive beside PUBLISH_BLOCK). Added the binding CLAUDE.md directive (tracker/feature-scope cycles MUST consult tracker-config.yaml; divergence vs programs.yaml/FEATURE_INDEX is a drift bug). Option-B independent audit PASS/L2. tsc 0; config 10/10 + route 8/8 (node harness) + 6/6 Playwright (real Chromium editor) + FX868 drift-guard node-verified; mocha env-blocked (host vscode-test mutex). Advisory only — no code/build/publish. _Prior: 2026-06-11 (RESEARCH - Tracker, Mind Map, and Shadow Genome remediation, META_LEDGER Entry #458. New `docs/research-brief-tracker-mindmap-shadow-genome-remediation-2026-06-11.md` is covered by the Tier 5 `docs/research-brief-*.md` glob. The cycle split seven remediation tracks and recorded `SG-VisualizationContractDrift` in `docs/SHADOW_GENOME.md`. Advisory only - no code/build/publish.)

**Last Reviewed**: 2026-08-23 (RESEARCH — **VS Code Agents window compatibility (Issue #83)**, META_LEDGER Entry #457. Validated the upstream-claim brief + `docs/research/VSCodeAgents-issue83.docx` against FailSafe source → new `docs/research-brief-vscode-agents-window-2026-06-10.md` (Tier 5, covered by the `docs/research-brief-*.md` glob). Two drifts surfaced: DRIFT-1 the brief's "FailSafe extension-contributed MCP server blocked by upstream #317460" is false (no `registerMcpServerDefinitionProvider` in source — FailSafe is an MCP client, not a server-def contributor); DRIFT-2 `extensions.supportAgentsWindow` opt-in is not yet shipped. Confirmed-real risk: single-`workspaceRoot` ledger resolution splits `.failsafe/` + `docs/META_LEDGER.md` across Agents-window worktree sessions. Recommendation: doc-first SHIELD plan (#83 Phase 0+1); worktree-aware ledger + setup command deferred to follow-on plans. SHADOW_GENOME pattern `SG-UpstreamClaimSurfaceMismatch` recorded. Advisory only — no code/build/publish. _Prior: 2026-06-10 (GOVERNANCE HOUSEKEEPING — **`docs/` Tier 4 → Tier 6 archive pass**. The targeted archive pass this index had scoped for a follow-up cycle: 42 historical plan/review/audit/handoff artifacts (38 `plan-qor-*` + `plan-devin-acp-enforce` + `review-acp-governance` + `FEATURE_INDEX_BASELINE_AUDIT` + `WHATS_NEXT`) moved out of public `docs/` to `.failsafe/archive/docs-cleanup-2026-06-10/` (Drift Contract item 4); `docs/compliance-education-component.md` kept + registered in Tier 5 (lasting compliance reference). `governance-index --enforce` now reports **zero** unregistered docs in `docs/`. Branch `chore/governance-index-archive-cleanup`; companion to the Monitor-theme seal (META_LEDGER Entry #448, PR #212). _Prior: 2026-06-10 (SUBSTANTIATE — **Monitor sidebar inherits the Console theme (FX881)**, META_LEDGER Entry #448. The embedded Console announces its active theme via a `failsafe.theme` postMessage; the Monitor sidebar inherits the shared design tokens — extracted to `theme-tokens.css`, **inlined from disk** by `FailSafeSidebarProvider` (NOT a cross-origin fetch, which the no-CORS localhost Console server would reject and silently leave unthemed) — and applies `data-theme`, so the sidebar chrome matches the operator's selected Console theme on boot and live on change; degrades to hardcoded `var()` fallbacks. Compact governed lifecycle (operator choice): /qor-plan → /qor-audit PASS → /qor-substantiate. Preceded this cycle by the qor-logic 0.106.0→0.109.0 upgrade (upstream **Qor-logic #199/#200/#201 CLOSED**; the #199 disclosed-pre-anchor tolerance cleared the #330/#331 manual-era band at the governance-health preflight — rc=0) + forward-only **RECONCILIATION Entry #447** attesting the disclosed #397/#401 duplicate-`previous_hash` fork (SG-ConcurrentLedgerRace-A), which cleared the substantiate Step 7.7 uniqueness check. tsc 0 · mocha 27 (2 emit tests + the FX868 CONSOLE_VERTICALS drift-guard) · Playwright command-center-settings 3/3 + monitor-theme-inheritance e2e 1/1 (live Console → host chrome `rgb(26,5,5)` + mirrored `data-theme`) + screenshot verified. Held local per Review Boundary (no version bump / tag / push). `seal_entry_check` rc=1 is only the upstream plan-name pattern (`plan-qor-phase<N>`) rejecting FailSafe's workspace plan name — the entry itself verifies (content/chain/prev recompute + uniqueness PASS + governance-health rc=0). `governance-index --enforce` surfaced **15 PRE-EXISTING unregistered `docs/plan-qor-*` docs** — out of scope, flagged for a separate index-cleanup cycle. _Prior: 2026-06-09 (SUBSTANTIATE — **Shadow Genome Map zoom + a11y + visual gate (#196 Phase 6 — FINAL, FX880)**, META_LEDGER Entry #446. The last #196 phase: Genome Map deterministic zoom in/out/reset (viewBox-based, clamped 0.5–3×, accessible names; §6.4); §15 a11y hardening (keyboard-operable nodes, control names, table fallback, reduced-motion, no color-only); the §17 visual gate realized as an **expanded SEMANTIC coverage suite** (zoom viewBox, keyboard nav, empty-graph, narrow-responsive) — pixel `toHaveScreenshot` baselines deliberately avoided (CI is Linux, authoring Windows). **This closes the #196 visual build (Phases 1–6, FX875–880).** tsc 0 · lint 0 errors · 11/11 builder + 4/4 route + **15/15 Playwright (real Chromium)** + full-suite build test (compile + bundle + test:ui 153 passed) + zoomed-map screenshot verified. PR #211 (now Phases 1–6, complete). _Prior: 2026-06-09 (SUBSTANTIATE — **Shadow Genome trust + federation + learning maturity (#196 Phase 5, FX879)**, META_LEDGER Entry #445. Render-ready surfaces (operator decision) for spec §8/§9/§10 — Trust Transitions (CBT/KBT/IBT chains), Federation (peer-state badges), Learning Maturity (6-stage funnel). None have a producer in the canonical FX863 graph yet, so they render REAL API data when present (fixture-tested) + honest empty/"not yet sourced" in production; learning-maturity "Observed" = failure-node count (sourced), deeper stages honest-0 (§8 "recording ≠ learning"). New `shadow-genome-panels.js` (55 lines). The upstream trust/federation/maturity DATA is a separate qor-logic dependency. tsc 0 · lint 0 errors · 11/11 builder + 4/4 route + **11/11 Playwright (real Chromium)** + screenshot verified. PR #211 (now Phases 1–5). _Prior: 2026-06-09 (SUBSTANTIATE — **Shadow Genome structural Genome Map (#196 Phase 4, FX878)**, META_LEDGER Entry #444. The causal-graph centerpiece (spec §6), `/frontend-design`-led — a **governance constellation**: governance gates anchor their failure nodes in a DETERMINISTIC radial layout (pure function of sorted ids; no force-sim/animation → stable screenshots). Backend: a trimmed `graph` (governance subgraph, id/type/label + edge {source,target,type}, no metadata). Frontend: new `shadow-genome-graph.js` (193 lines) — typed nodes/edges (severity colors, anchor rings, dashed insufficient-evidence, arrow markers), click-select highlight + inspector, legend, and a **"View as Table"** a11y fallback (graph is not the only access path, §15). tsc 0 · lint 0 errors · 9/9 builder + 4/4 route + **8/8 Playwright (real Chromium)** + constellation screenshot verified. PR #211 (now Phases 1–4). _Prior: 2026-06-09 (SUBSTANTIATE — **Shadow Genome incident ledger + case-file drawer (#196 Phase 3, FX877)**, META_LEDGER Entry #443. The operational Incidents surface (spec §7), `/frontend-design`-led. Backend: `GovernanceDashboardResponse` gains an `incidents[]` array (`IncidentSummary`) derived from the governance-subgraph failure nodes (recurrence = incident-edge count; `governanceRoots`; severity active≥3/repeated≥2/emerging) — no new persistence, no faked data. Frontend: an evidence-ledger table (severity-spined, keyboard-navigable rows) opening a slide-over **case-file drawer** (what failed / governance roots / recurrence / node id / honest `not yet sourced` for remediation+trust) with Locate-in-Genome → Genome Map. Token-only `.sg-incident-*`/`.sg-drawer-*` CSS. tsc 0 · lint 0 errors · 7/7 builder + 4/4 route + **5/5 Playwright (real Chromium)** + screenshot visually verified vs the Mythiq mockup. PR #211 (now Phases 1+2+3). _Prior: 2026-06-09 (SUBSTANTIATE — **Shadow Genome dashboard sub-view (#196 Phase 2, FX876)**, META_LEDGER Entry #442. Governance › Shadow Genome — the dashboard sub-view over the Phase-1 `/api/qor/governance-dashboard` API: `ShadowGenomeRenderer` as the 4th `governance` TabGroup sub-pill (NOT a top-level tab — the 7 Console verticals are pinned by the FX868 drift-guard; the shadow genome is a Governance concern). Header + LIVE/degraded chip, 5 summary cards, 4-mode nav (Genome Map / Incidents / Trust Transitions / Federation) where each mode renders its REAL slice of the API with honest empty states (no placeholder bodies); Mythiq token-only `.sg-*` CSS; spec §14 degraded card. **Visual gate: 3/3 Playwright green (real Chromium) + screenshot verified against the Mythiq mockup.** Folded onto the Phase-1 branch / PR #211. _Prior: 2026-06-09 (SUBSTANTIATE — **Shadow Genome dashboard API (#196 Phase 1, FX875)**, META_LEDGER Entry #441. Read-only `GET /api/qor/governance-dashboard` over the shipped FX863 data layer: pure `buildGovernanceDashboard` derives `GovernanceDashboardResponse` solely from `governanceSubgraph`+`summarizeGenome` (no persistence, canonical node/edge semantics only), degrade-safe (off/absent loader → `enabled:false` zeroed); `trustTransitions`/`federation` honest-empty until spec Phase 5. Backend-only slice (Playwright visual + a11y gate at spec Phase 6). Design source operator-supplied (`docs/design/SHADOW_GENOME_UI.md` `17bd813` + Mythiq mockup); placement = standalone **Governance** domain (operator-corrected, NOT Agents). Audit PASS (L2, all 14 passes); tsc/eslint 0 errors; 9/9 mocha green. Sealed on the repaired ledger (#440 via PR #210). NOTE: `governance-index --enforce` surfaced **8 PRE-EXISTING unregistered docs** (`docs/plan-qor-phase61/62`, `plan-qor-sentinel/stale-cache/v5-1-0/v5-1-8/voice`, `review-acp-*`) — out of #196 scope; flagged for a separate index-cleanup cycle. Held local on `feat/shadow-genome-ui-196-phase1`. _Prior: 2026-06-09 (REMEDIATE — **META_LEDGER UTF-8 chain repair (Entry #420) + re-chain #419→#440**, META_LEDGER Entry #440. Operator-approved fix of a sealed-in 19-byte codepoint-truncation in Entry #420 (DELIVER v5.5.2) that made the ledger invalid UTF-8 and blocked the entire governance toolchain; oracle-proven (SHA-256 of the corrupted body == stored Content Hash → corruption predated the seal), deterministic re-chain of #419→#440; backup at `docs/META_LEDGER.md.pre-chain-repair.bak`. Repair made the ledger readable for the first time, surfacing a **pre-existing manual-era residual** (#330 chain-math, #331 placeholder, #338/#339 chain-math) that the strict skill-entry gate cannot tolerate (single-lineage; both `verify()` tolerance paths require duplicate-`previous_hash`). Operator chose the **upstream qor-logic fix** (no seal rewrite) — the skill-entry gate lacks `verify_post_anchor`'s disclosed-pre-anchor tolerance; draft brief + two further upstream findings (SHADOW_GENOME `"TODO"` substring false-positive; seal-step admits non-UTF-8) in `.failsafe/governance/UPSTREAM_QORLOGIC_FINDINGS_2026-06-09.md`. Three `/qor-auto-dev-1` cycles queued but blocked at preflight until the gate clears. _Prior: 2026-06-08 (SUBSTANTIATE — **Release-readiness consolidation: doc currency + qor-logic 0.103.1 skill sync**, META_LEDGER Entry #439. Living docs brought current with the tracker thread (CHANGELOG [Unreleased] + SYSTEM_STATE + this index); the governed SHIELD skills re-synced to qor-logic 0.103.1 via `qor-logic install --host claude --scope repo` — notably the new `/qor-substantiate` **surface-tag lint** (Qor-logic#196, the upstream gate filed during #206), verified live against FailSafe (`feature_index_surface: ok`). Staged for the next release. _Prior: SUBSTANTIATE — **Tracker render consumption / A.2b-visual** (#202, FX872–FX874), META_LEDGER Entry #438. The visual half of A.2b: per-vertical Coverage bar from `featureStats`, program bars that count unanchored (projected) phases (a model-contract under-reporting fix), a `manifestSource`-aware advisory; additive to the App-lineage shell; Playwright 9/9 real-chromium + operator visual sign-off; PR #208 `--admin`-merged. _Prior: SUBSTANTIATE — **FEATURE_INDEX Surface column + per-surface governed attribution** (#206, FX869–FX871), META_LEDGER Entry #437. A `Surface` column on all 689 rows (the 7 Console verticals + `platform`) + `attributeFeatures` per-surface `featureStats` — surface↔feature mapping as governed evidence (accurate by construction); upstream gate Qor-logic#196; PR #207 `--admin`-merged. _Prior: SUBSTANTIATE — **Live `programs.yaml` = the 7 Console surfaces + graceful no-server message** (#205), META_LEDGER Entry #436. Served operator manifest rewritten so the live tracker tells the truth (verticals = the 7 surfaces, programs = the same 7, evidence-anchored) + an open-from-Console message on the file:// no-server path; PR #205 `--admin`-merged; #198 closed (superseded by #204). _Prior: SUBSTANTIATE — **Tracker verticals = the 7 Console surfaces** (accuracy correction, FX868), META_LEDGER Entry #435. Operator-raised root-cause fix: verticals are the PRODUCT's user-facing surfaces, not code directories. FX862 grouped FEATURE_INDEX by code dir (an implementation taxonomy that misrepresented the product); replaced with the authoritative 7 = the Console Center tabs (`command-center.html` nav + `command-center.js` renderers): Monitor · Learn · Agents · Governance · Workspace · Integrations · Config, each carrying its real sub-views as functionality. Pinned as `CONSOLE_VERTICALS` + a **drift-guard test** that parses the real command-center.js and fails on divergence, so the projection can never silently lie about the product. 24 unit tests + real-data smoke (exactly the 7). Held local on `feat/tracker-verticals-console-spine`; awaits `--admin` merge. _Prior: SUBSTANTIATE — **Tracker route consumes the governance projection** (A.2b-data, #202), META_LEDGER Entry #434. The data half of A.2b: `TrackerRoute.api` projects the manifest from the governance ledger (META_LEDGER + FEATURE_INDEX + plans, via the FX865 nodeSidecarDeps seam) when the operator `programs.yaml` is absent on a governed repo (operator stays authoritative; new `manifestSource` field). **Unanchored-tolerant reconciliation** (operator decision): empty rc → `phase-unanchored` WARN not abort; `phasesFromPlans` anchors to a Target Version only when it is a real release (axis-aware via `knownReleaseIds`), so never-shipped versions like v4.9.3 degrade instead of aborting. 44 unit tests + 2 real-data smokes (operator path no-regression; projection path 10 programs / 61 phases / 12 verticals / 61 decisions, 4 anchored, 57 unanchored, **0 aborts → ok:true**). Backend — the client `cumulative()` fix + Accountable-reference visual fidelity are the deferred A.2b-visual half. Held local on `feat/tracker-projection-consume-202`; awaits `--admin` merge. _Prior: SUBSTANTIATE — **Plans projection + vertical taxonomy polish** (A.1b, #195/#198), META_LEDGER Entry #433. Completes the tracker-as-governance-sidecar PROJECTION: `parsePlans` reads `.failsafe/governance/plans/*.md` → **programs** (theme buckets, singletons fold to "Other", ~10 not 61) + **phases** (one per plan, rc-anchored to Target Version) — the last + least-structured source, filling what A.1 left empty. **#198**: `areaOf` build-prefix normalization + NON_CAPABILITY denylist + file-extension heuristic drop stray verticals (real repo 15 → 12 clean areas). Wired through `GovernanceSources.plans` + the FX865 sidecar (`SidecarDeps.readPlans`). 16 unit tests + real-data smoke (13 releases / 10 programs / 12 verticals / 60 decisions). Backend logic — render consumption stays A.2b (visual gate). Held local on `feat/plans-projection-195`; awaits `--admin` merge. _Prior: SUBSTANTIATE — **Tracker governance-sidecar emission** (A.2, #194, FX865), META_LEDGER Entry #432. Materializes the FX862 governance projection to a GENERATED `docs/roadmap/programs.generated.yaml` sidecar — pure `emitGovernanceSidecar(SidecarDeps)` over META_LEDGER + FEATURE_INDEX; **HARD INVARIANT: never reads/writes the operator `docs/roadmap/programs.yaml` (FX859)**; degrade-safe + idempotent (skip-if-unchanged). Two triggers: the `failsafe.emitGovernanceSidecar` command + opt-in WorkspaceMutationBus auto-emit (`failsafe.tracker.emitGovernanceSidecar`, default off). 6 unit tests + real-data smoke (this repo: 12 releases / 15 verticals / 58 decisions). Backend logic — render consumption deferred to A.2b (carries the visual gate). Held local on `feat/governance-sidecar-emission-194`; awaits `--admin` merge. _Prior: DELIVER — **v5.6.3 dual-marketplace publish** (Governance bundle: PR↔issue linkage governance + tracker GovernanceProjection sidecar + shadow-genome consumer), META_LEDGER Entry #431. Four governance features FX861–FX864 shipped in one patch — three views of the same governance ledger (the convergence in the research brief). Release PR #193 `--admin`-merged to main (tag `v5.6.3` `e716767`); both publish jobs (VS Code Marketplace + Open VSX) succeeded, production-gate operator-approved; Build & Test passed first-run (FX525 de-flake held); Open VSX query-API ingestion lag at seal (publish job authoritative, no reshape). Closes #154, #118. Deferred follow-ups filed: #194 (A.2 sidecar emission), #195 (A.1b plans→phases), #196 (#118 UI), #197 (shared GovernanceProjection substrate), #198 (taxonomy polish). _Prior: SUBSTANTIATE — **PR-linkage Check Run live-wiring** (#154 part 2, FX864), META_LEDGER Entry #430. Completes FX861 (the linkage auditor was built but inert): a generic `publishCheckRunPayload` (extracted from `publishCheckRun`, behaviour-preserving) + `defaultGitHubGet` + a `publishLinkageCheck` bridge + command glue now publish a PR-linkage Check Run beside the SHIELD verdict. 8 new tests (27 github-checks green); off-by-default, token header-only. Held local on `feat/pr-linkage-publish-154`. #118 (FX863) MERGED to main (`a7f3f79`). Four governance features FX861–FX864 queued for the v5.6.3 bundle. _Prior: SUBSTANTIATE — **Shadow-genome client** (#118, FX863), META_LEDGER Entry #429. First slice of #118 (UNBLOCKED — qor-logic's `shadow_genome_graph` shipped, #139 closed): the DATA layer consuming the causal governance graph (pure `parseGenomeGraph`/`summarizeGenome`/`governanceSubgraph` + off-by-default injected-RunCommand `loadShadowGenome`; UI deferred). 7 unit tests + real-Python smoke; adjacent to the tracker projection (FX862) — both are governance-ledger views. Held local on `feat/shadow-genome-viewer-118`. A.1 (FX862, #428) MERGED to main (`0e54563`). Three governance features (FX861/862/863) queued for the v5.6.3 bundle. _Prior: SUBSTANTIATE — **Tracker GovernanceProjection reader** (A.1, FX862), META_LEDGER Entry #428. First slice of the tracker-as-governance-sidecar direction (research brief in `.failsafe/governance/`): pure `governance-projection.ts` projects a `TrackerManifest` from META_LEDGER (releases + decisions) + FEATURE_INDEX (verticals) — the governed-repo authoritative source; FX857 generator stays the ungoverned fallback; FX859 categorization on top. 6 unit tests + real-data smoke (12 releases / 15 verticals / 54 decisions; the smoke caught a mixed-Code-column drift, 383→15 verticals). Held local on `feat/governance-projection-tracker`. #154 (FX861) MERGED to main (`5aa4cee`). _Prior: SUBSTANTIATE — **GitHub PR↔issue linkage governance auditor** (#154), META_LEDGER Entry #427. Pure `pr-linkage-audit.ts` (FX861) — GitHub's 9 close-keywords applied per-number so `Closes #1, #2` flags `#2` as a silent non-close (the footgun that stranded #166/#167/#172/#174); findings → deterministic Check Run conclusion + off-by-default injected-transport live path, beside the #96 SHIELD check. 9 unit tests; backend logic, no Playwright gate. Held local on `feat/pr-linkage-governance-154`; awaits `--admin` merge + release decision. _Prior: DELIVER — **v5.6.2 publish** (Tracker loading/freshness + FX525 de-flake), META_LEDGER Entry #426. Tracker loading skeleton + "last refreshed" + manual Refresh (#163, FX860) + the FX525 test-teardown de-flake (`close()` now calls `server.stop()`). PR #187 `--admin`-merged (tag `v5.6.2` `87d3e9a`); Build & Test passed on the first pipeline run (the de-flake held — no dead-tag, contrast v5.6.1); published (Open VSX live at 5.6.2; VS Code Marketplace job succeeded, query-API ingestion lag at seal). Closes #163. _Prior: SUBSTANTIATE — **Tracker loading/freshness (#163) + FX525 de-flake** for v5.6.2, META_LEDGER Entry #425. Tracker dashboard loading skeleton + "last refreshed" stamp + manual Refresh (FX860, pure client-side, no server contract change); FX525 teardown de-flake (root cause: leaked ConsoleLifecycleService ledgerWatcher; the test helper `close()` now calls `server.stop()` — 36/36 under 4x3 stress, fixing the flake that dead-tagged the v5.6.1 pipeline). Held local on branch `feat/tracker-loading-freshness-163`; awaits bump + `--admin` merge + `production` publish for v5.6.2. _Prior: DELIVER — **v5.6.1 dual-marketplace publish** (Development Tracker for any repo), META_LEDGER Entry #424. Non-semver render resilience + manifest generator + Bicameral-MCP Layer 3 enrichment + operator-confirmed taxonomy. Published to VS Code Marketplace + Open VSX (tag `v5.6.1` `d3d7f57`, both verified live at 5.6.1; production-gate approved; Build & Test cleared one pre-existing FX525 teardown flake on re-run). PRs #183 + #184 `--admin`-merged. Closes #174/#172/#167/#166. _Prior: SUBSTANTIATE — **Development Tracker generator + operator categorization** (#174), META_LEDGER Entry #423. Non-semver (PR-incremental) render resilience + manifest generator (PRs → CHANGELOG → programs.yaml) + Bicameral-MCP Layer 3 enrichment + operator-confirmed taxonomy (keep/drop/rename/fold). `tracker-dashboard.spec.ts` 3/3 in real chromium closes the user-facing-visual e2e gate the prior render fix lacked. FX857–FX859 + e2e on FX847. Held local on branch `feat/tracker-manifest-generator` (PR #183, supersedes #182); awaits operator bump + `--admin` merge + `production` publish. Current release v5.6.0 (DELIVER #422, ACP governance). _Prior: 2026-06-04 (DELIVER — **v5.5.1 dual-marketplace publish** (Maintenance), META_LEDGER Entry #419. Squash-merged via PR #162; published to VS Code Marketplace + Open VSX (tag `v5.5.1` `f130701`, both publishes production-gate-approved, GitHub Release auto-created). Four evaluated Dependabot Action bumps to Node.js 24 runtimes (checkout v6.0.3, setup-node v6.4.0, upload-artifact v7.0.1, download-artifact v8.0.1, SHA-pinned) + Development Tracker error-surfacing fix; the upload-v7 → download-v8 publish hand-off validated end-to-end; PRs #155–#158 incorporated. Issues #161 (Windsurf compat) + #163 (tracker progress/freshness) filed; CodeQL ruleset confirmed an intentional human-gate (merges used operator `--admin`). _Prior: DELIVER — **v5.5.0 dual-marketplace publish** (Integration Suite), META_LEDGER Entry #418. Squash-merged via PR #153 + release PR #159; published to VS Code Marketplace + Open VSX (tag `v5.5.0` `201152a`, both publishes production-gate-approved, GitHub Release auto-created). /qor-substantiate VETO remediation caught 4 external-name defects against live docs (Aider `--yes-always`, Sentry env-in-query, OpenHands real-SDK shape, Linear case-insensitive); 15 per-integration READMEs + `INTEGRATION_STRUCTURE_STANDARD.md` (Tier 5) + FEATURE_INDEX FX826–FX836; closes #90/#96/#97/#98/#101/#102/#104/#105/#106/#107; enhancement #154 filed; merges used operator-authorized `--admin` (CodeQL ruleset still blocks). _Prior: Registered `docs/integrations/INTEGRATION_DOCS_INDEX.md` — maintained documentation-links registry for every integration (grounding for verify-external-names) — added in the #102 Sentry ingest cycle. _Prior: 2026-06-03 (DELIVER, META_LEDGER Entry #417 — v5.4.3 dual-marketplace publish: **Development Tracker** (B-INT-17, premium data-driven dashboard at `/console/tracker`, embedded in the Workspace tab; full-history CHANGELOG discovery + major-default timeline zoom + per-release traceable record + tiered program-progress) + **Agent Governance Toolkit installer** (B-INT-16, env-auto-detected Integrations sub-view). Tag `v5.4.3` `2c7b49c`; both publishes by CI (production-gate approved); GitHub Release **auto-created** by the new `github-release` job. Build & Test cleared one `idle-scheduler` timer flake on re-run. A new `code_scanning`/`code_quality` main ruleset (unconfigured CodeQL) blocked the merge until an operator ruleset bypass. BACKLOG v5.4.3 → RELEASED. PRs #140 + #141 + #142. _Prior: Entry #416 — v5.4.2 Integration Expansion._) v5.4.2 RELEASED to VS Code Marketplace + Open VSX via tag `v5.4.2` `fdbf779` (both publishes by CI, approved at the `production` reviewer gate); GitHub Release object created (page had frozen at v4.8.0) + `github-release` job added to `release.yml` (PR #136). Two prior tags died in the pipeline and were left as forward-only dead tags: v5.4.0 `1563c80` (stale B-INT-5 pill test) + v5.4.1 `02a7d8f` (VS Code stable auto-update 1.122.1→1.123.0 broke the `better-sqlite3` native rebuild; fixed by pinning the VS Code test version). Batch: SARIF #99, MCP Registry #108, Slack #100, Context7/Mermaid installers B-INT-13/14, Dev Tracker v1/v1.1, B-SUBSTRATE-2/3, research gate #109. Closed #99/#100/#108/#95/#109. BACKLOG v5.4.0/v5.4.1 → DEAD TAG, v5.4.2 → RELEASED. Merge chain: PRs #121–#136. _Prior: Entry #415 — v5.3.3 Integration Beta._))
**Maintainer:** every cycle that touches governance artifacts (write-time obligation, not periodic chore)
**Purpose:** Authoritative hierarchical index of every governance artifact in this repository. Every governance doc MUST be registered here. Drift in this index = governance drift.

> **Why this exists.** Governance docs accumulated across 6 directories (`./`, `docs/`, `.failsafe/governance/`, `.failsafe/governance/plans/`, `.agent/staging/` deprecated, `.failsafe/archive/`). Without an index, individual docs went stale silently. One incorrect value in a stale governance doc can cascade — e.g., a stale `SYSTEM_STATE.md` version line drives a `/qor-status` mis-routing → wrong skill invoked → cycle starts from wrong baseline. The index is the canonical map; the freshness contract per tier is the drift gate.

## Tier model

Every governance artifact belongs to exactly one tier. The tier determines:
- **Freshness contract** — when staleness is a bug.
- **Review cadence** — when an automated or manual sweep MUST happen.
- **Drift signal** — what a reader sees when this doc is wrong.
- **Owner skill** — which `/qor-*` skill is responsible for keeping it current.

| Tier | Name | Freshness contract | Review cadence | Drift signal |
|---|---|---|---|---|
| 1 | Canonical Source | MUST be current at every cycle close | every `/qor-substantiate` + every `/qor-repo-release` | wrong version, wrong state, missing recent entries |
| 2 | Doctrine & Policy | stable; changes are explicit doctrine events | quarterly + on operator policy reversal | rules contradict each other or contradict CLAUDE.md memory |
| 3 | Active Initiative | live until close; ages out at substantiate | cycle-close gate + before next cycle resumes the initiative | references shipped features as "pending", or vice versa |
| 4 | Per-Plan Artifact | live for plan duration; archived at substantiate | seal-time | plan shipped but artifact still presents as "open" |
| 5 | Reference Material | informational, slow-drift | semi-annual | factual claims diverge from current code/contracts |
| 6 | Archived | frozen historical record | annual chain-integrity spot-check | none (frozen by definition) |

---

## Tier 1 — Canonical Sources

Always-current. A stale Tier 1 doc IS a P0 governance bug.

| Path | Purpose | Owner skill | Drift signal |
|---|---|---|---|
| `CLAUDE.md` | Operator instructions + project memory references | manual + memory sync | references shipped doctrine as pending or vice versa |
| `GOVERNANCE.md` | SHIELD framework definition (S/H/I/E/L/D phase contracts) | `/qor-bootstrap` | phase contracts diverge from installed `/qor-*` skills |
| `AGENTS.md` / `GEMINI.md` / `codex.md` / `.github/copilot-instructions.md` | Cross-agent root pointers (Codex / Gemini / Copilot / Cursor / Windsurf etc.) | `/qor-bootstrap` re-scaffold | new agent SDK released, no entry here |
| `README.md` (root) | Public-facing positioning + Current Release marker | `/qor-document` RELEASE_METADATA + `/qor-repo-release` | version marker behind `package.json` |
| `FailSafe/extension/README.md` | Extension-marketplace README + Current Release marker | `/qor-document` RELEASE_METADATA + `/qor-repo-release` | version marker behind `package.json` |
| `CHANGELOG.md` (root + extension copy) | Release notes per version | `/qor-document` RELEASE_METADATA | DELIVER entry in `META_LEDGER` without matching CHANGELOG section |
| `docs/CONCEPT.md` | Genesis intent — what FailSafe is and is not | `/qor-bootstrap`; mutations require `/qor-ideate` + audit | drift from public framing in README/CHANGELOG |
| `docs/ARCHITECTURE_PLAN.md` | Current architecture baseline + Risk Grade | `/qor-plan` updates; `/qor-audit` reviews | components shipped but not described, or vice versa |
| `docs/META_LEDGER.md` | Merkle-chained decision ledger; **chain integrity is load-bearing** | `/qor-substantiate` (appends) + `/qor-validate` (verifies) | chain hash mismatch, missing entries, gaps in numbering |
| `docs/SYSTEM_STATE.md` | Living snapshot of seal-state, current release, active integrations | `/qor-document` SESSION_DOCS + `/qor-substantiate` (post-seal update) | "Last Updated" older than latest sealed entry |
| `docs/BACKLOG.md` | All backlog items + version summary table | every cycle that ships, audits, or closes an item | row status diverges from `META_LEDGER` DELIVER |
| `docs/FEATURE_INDEX.md` | Cross-reference of every feature → source / docs / test / verification status | `/qor-substantiate` (FEATURE_INDEX gate) | feature shipped without FX entry OR FX `unverified` for shipped feature |
| `docs/GOVERNANCE_INDEX.md` (this file) | Authoritative map of every governance artifact in this repo | every cycle that touches governance | new governance doc created without index entry |
| `package.json` (root + extension) | Authoritative version source — every other version marker derives from this | `/qor-repo-release` release-gate.cjs | other version markers ahead of or behind this |
| `docs/roadmap/tracker-config.yaml` | Operator-declared tracker taxonomy (programs ∥ verticals + agent mappings) — the source of truth cycles consult per the CLAUDE.md directive (FX891) | Workspace › Taxonomy editor (`POST /api/v1/tracker/config`); manual edits ok | declared taxonomy diverges from `docs/roadmap/programs.yaml` / FEATURE_INDEX surfaces |

---

## Tier 2 — Doctrine & Policy

Stable rules. Changes are explicit doctrine events recorded in `META_LEDGER` and operator memory.

| Path | Purpose | Owner |
|---|---|---|
| `CODE_OF_CONDUCT.md` | Community conduct rules | manual (rare) |
| `CONTRIBUTING.md` | External contributor workflow | manual |
| `SECURITY.md` | Security vulnerability disclosure process | manual |
| `.failsafe/governance/PUBLISH_BLOCK.md` | Active publish-block flag + lift conditions | `/qor-repo-release` checks; `/qor-substantiate` lifts |
| `.failsafe/governance/tracker-taxonomy.directive.md` | Emitted record of the operator tracker taxonomy + MUST-CONSULT clause (FX891) — advisory (CLAUDE.md is the binding mechanism; no CI gate yet) | Workspace › Taxonomy editor emits on save; gitignored (local record) |
| `.failsafe/governance/doctrine-shadow-genome-countermeasures.md` | Shadow Genome pattern doctrine (SG-* IDs) | `/qor-process-review-cycle` |
| `docs/AUTONOMOUS_RELIABILITY_MANIFEST.md` | Reliability gate posture across phases | `/qor-audit` references |
| `docs/CORE_AXIOMS_AND_RULES_COMPILED.md` | Compiled axioms + rules from doctrine | manual sync |
| `docs/FILE_STRUCTURE_STANDARD.md` | Where each kind of file belongs | manual; `/qor-organize` enforces |
| `docs/governance-cache-invalidation.md` | Cache invalidation discipline doctrine | manual |
| `docs/governance-mode-transitions.md` | Mode-transition state machine doctrine | manual |
| `docs/PROCESS_SHADOW_GENOME.md` + `PROCESS_SHADOW_GENOME_UPSTREAM.md` | Process-level failure pattern catalogue | `/qor-shadow-process` |
| `docs/REPO_GOVERNANCE.md` | Repo-level governance posture | manual |
| `docs/SHADOW_GENOME.md` | Code-level shadow genome catalogue | `/qor-meta-track-shadow` |
| `docs/MINIMAL_HOOKS_GUIDE.md` | Hooks doctrine for Claude Code settings | manual |
| `docs/PLUGIN_INTERFACE_PROPOSAL.md` | Plugin interface design proposal | manual |
| Operator memory at `~/.claude/projects/.../memory/feedback_*.md` | Durable operator-stated rules | session-level; only the operator may edit |

---

## Tier 3 — Active Initiative Tracking

Live until close. Must be archived at substantiate, or moved to Tier 4 if they become per-plan.

| Pattern | Location | Lifetime |
|---|---|---|
| `SESSION_STATE_*.md` | `.failsafe/governance/` | from cycle start to substantiate or pause |
| `REMEDIATION_*.md` | `.failsafe/governance/` | from remediation proposal to next seal |
| `RESEARCH_BRIEF_*.md` (active) | `.failsafe/governance/` | from research close to plan supersede or substantiate |
| `IDEATION_*.md` | `.failsafe/governance/` | from `/qor-ideate` to plan supersede |
| `CONTENT_MATRIX_*.md` | `.failsafe/governance/` | from authoring start to substantiate |
| `entry-<N>-body.md` (newest) | `.failsafe/governance/` | from authoring to META_LEDGER append (then optional retention as reference) |

**Current Tier 3 occupants (as of 2026-05-28 housekeeping):**

- `SESSION_STATE_open-design-integration-v1.1.md` (recent seal at #405; keep until next post-v1.1 cycle reviews)
- `SESSION_STATE_bicameral-mcp-integration.md` (Phase 4-5 carryover at `.failsafe/archive/SESSION_STATE_bicameral-mcp-integration.md`)
- `REMEDIATION_open-design-v1.1-2026-05-27.md`
- `RESEARCH_BRIEF_open-design-integration-{,-v1.1,-v2-model2}-2026-05-27.md`
- `RESEARCH_BRIEF_qor-substrate-modules-v1-2026-05-27.md`
- `RESEARCH_BRIEF_bicameral-mcp-integration.md` + `AUDIT_REPORT_bicameral-mcp-integration.md`
- `RESEARCH_BRIEF_failsafe-learn-swe-craft.md` + `CONTENT_MATRIX_failsafe-learn-swe-craft.md` + `IDEATION_failsafe-learn-swe-craft.md`
- `IDEATION_educational-component.md`
- `RESEARCH_BRIEF_procedural-failure-signal-channel.md`
- `entry-{395..406}-body.md` (META_LEDGER source bodies; retain through next housekeeping cycle)

---

## Tier 4 — Per-Plan Artifacts

Plans and their immediate audit/implementation/substantiation reports. Archive at substantiate-seal-time.

| Pattern | Location | When to archive |
|---|---|---|
| `plan-*.md` (root) | `./` | at substantiate seal of the plan |
| `plan-*.md` (workspace) | `docs/` | at substantiate seal of the plan |
| `plan-*.md` (mirror) | `.failsafe/governance/plans/` | at substantiate seal of the plan |
| `AUDIT_REPORT_<plan>.md` | `.failsafe/governance/` | at substantiate of that plan |
| `IMPLEMENTATION_REPORT_<plan>.md` | `.failsafe/governance/` | at substantiate of that plan |
| `SUBSTANTIATE_REPORT_<plan>.md` | `.failsafe/governance/` | at next housekeeping cycle |
| Plan iteration drafts (v1/v2 superseded by final v3 etc.) | as above | at audit PASS of final iteration |

**Outstanding Tier 4 cleanup:**

- **DONE 2026-06-10**: the `docs/` archive pass — 42 historical plan/review/audit/handoff artifacts (38 `plan-qor-*` + `plan-devin-acp-enforce` + `review-acp-governance` + `FEATURE_INDEX_BASELINE_AUDIT` + `WHATS_NEXT`) moved to `.failsafe/archive/docs-cleanup-2026-06-10/` (Tier 4 → Tier 6, Drift Contract item 4). `docs/compliance-education-component.md` was kept and registered in Tier 5 (lasting compliance reference). `governance-index --enforce` now reports zero unregistered docs in `docs/`.
- ~50 plan-*.md files in `.failsafe/governance/plans/`, many for already-shipped versions (v4.6.0, v4.9.5, v5-round1-4, skill-consolidation v1/v2 superseded by v3, v4.10.1 iterations that never publishably shipped). These are already inside the gitignored governance tree (not public `docs/`), so they are not enforce-flagged; a tidy-up of that mirror dir remains a low-priority follow-up.

---

## Tier 5 — Reference Material

Informational. Slow-drift. Reviewed semi-annually unless cited by an active cycle.

| Path | Purpose |
|---|---|
| `FILE_INDEX.md` (root) | Repo file inventory (informational; SYSTEM_STATE supersedes for governance state) |
| `docs/EDUCATION.md` | Educational subsystem design reference |
| `docs/compliance-education-component.md` | Compliance determination for the FailSafe Learn (SWE craft) component — retained as a lasting compliance reference |
| `docs/FAILSAFE_SPECIFICATION.md` | Top-level product specification |
| `docs/INTEGRATIONS.md` | Integration surface documentation (Bicameral + Open Design) |
| `docs/integrations/INTEGRATION_DOCS_INDEX.md` | Maintained registry of official docs for every integration; canonical source for verify-external-names; per-cycle refresh required |
| `docs/LEARN_TAB.md` | Learn tab design reference |
| `docs/ROADMAP.md` | Forward-looking roadmap (not authoritative for current state — that's `SYSTEM_STATE`) |
| `docs/UI_MANIFEST.md` | UI module manifest |
| `docs/SUBSTRATE_MODULES.md` | qor.scripts substrate v1 reference (added v5.3.0) |
| `docs/VIBE_CODER_PLAYBOOK.md` | Audience-targeted onboarding playbook |
| `docs/test-patterns.md` | Test pattern catalogue |
| `docs/research-brief-*.md` (historical) | Sealed research briefs retained for back-reference |
| `docs/VS_CODE_AGENTS_WINDOW.md` | #83 Phase-0 Agents-window snapshot + UNVERIFIED compatibility matrix, date-pinned to VS Code 1.128 / 2026-07-13. FAST-DRIFT exception to this tier's cadence — stale when ANY §6 trigger fires (upstream preview banner drops; microsoft/vscode#318103 leaves Backlog; any agentsWindow API finalizes) or when the observation date exceeds one VS Code stable cycle without revalidation (META_LEDGER Entry #466 is the revalidation template) |
| `docs/ecosystem-position.md` | #232 workstream 7: FailSafe's seat in the AI-assisted SDLC (governance stack, Pro sibling, agent seams, trust posture). Public-sources-only; confidentiality-bounded per the v5-reveal policy. Drift signals: Pro positioning/link changes (B208), adapter-boundary contract changes, new agent-protocol seam ships |
| `docs/agents/issue-tracker.md` | Issue-tracker contract for the vendored mattpocock/skills engineering pack (Wayfinding operations: `wayfinder:map`, sub-issues, native dependencies) + FailSafe-notes footer keeping qor doc-topology + SHIELD precedence. Vendored source pinned in `.claude/skills/MATTPOCOCK-SKILLS-LICENSE` (local-only). Registered 2026-08-19 (META_LEDGER #514-#515) |
| `docs/release-runbook-v5-1-0.md` | Historical release runbook (kept as template for future runbooks) |
| `.failsafe/governance/FEATURE_INVENTORY_CODE.md` + `FEATURE_INVENTORY_DOCS.md` | Inventory snapshots (slow-drift) |
| `.failsafe/governance/DEPLOYMENT_TELEMETRY_ROADMAP.md` | Forward roadmap for runtime telemetry |
| `.failsafe/governance/PROPRIETARY_SKILLS_MIGRATION_STATUS.md` | Migration status reference |
| `.failsafe/governance/host-registry.schema.json` | Host registry schema |
| `.failsafe/governance/RESEARCH_BRIEF_skill-consolidation.md` | Skill consolidation research (cited in CLAUDE.md memory) |
| `.failsafe/governance/RESEARCH_BRIEF_agent-failsafe-current-state.md` | Agent-side current-state research |
| `.failsafe/governance/RESEARCH_BRIEF_agt-sre-architecture.md` | Microsoft AGT SRE adapter target reference |
| `.failsafe/governance/BROWSER_VERIFICATION.md` | Browser verification procedure reference |
| `docs/README.md` | Index of the `docs/` tree — what lives where, for a reader arriving cold |

---

## Tier 6 — Archived

Frozen historical record. Do not modify. Read-only.

| Pattern | Location | Provenance |
|---|---|---|
| `.failsafe/archive/2026-05-stale-cleanup/{agent-staging,governance-root,v5.1.x-shipped,plan-artifacts-shipped,entry-drafts}/` | (gitignored) | Created 2026-05-28 by `/qor-auto-dev-1` housekeeping cycle; 21 files relocated from `.agent/staging/` (deprecated path) and `.failsafe/governance/` root |
| `.failsafe/archive/AUDIT_REPORT_2026-03-11_*.md`, `AUDIT_REPORT_2026-05-05_*.md` | (gitignored) | Pre-existing archives |
| `.failsafe/archive/SESSION_HANDOFF_2026-*.md`, `SESSION_STATE_*.md`, `SESSION_CHANGE_REPORT_*.md` | (gitignored) | Pre-existing archives |
| `.failsafe/archive/IMPLEMENTATION_PLAN.md`, `GapAudit.md`, `LEDGER_UPDATE.md`, `MANIFEST_MIGRATION.md`, `REORGANIZATION_SUMMARY.md` | (gitignored) | Pre-existing archives |
| `.failsafe/archive/plan-*.md` (historical) | (gitignored) | Pre-existing |
| `.failsafe/archive/quarantine-retired/`, `stale-configs/` | (gitignored) | Pre-existing quarantine bins |

---

## Drift Detection Contract

Every cycle that produces governance writes (`/qor-substantiate`, `/qor-repo-release`, `/qor-document` RELEASE_METADATA, `/qor-bootstrap`) MUST:

1. **Verify Tier 1 coverage** — every Tier 1 doc has a "Last Updated" or equivalent freshness marker. Any marker older than the most recent sealed `META_LEDGER` entry is a drift bug; fix before declaring the cycle done.
2. **Register new artifacts** — any new doc written under `./`, `docs/`, `.failsafe/governance/`, or `.failsafe/governance/plans/` MUST be added to the appropriate tier table in this file in the same commit that creates it.
3. **Move Tier 3 → Tier 6 at seal** — substantiate-time obligation: every `SESSION_STATE_*`, `REMEDIATION_*`, `IDEATION_*`, `CONTENT_MATRIX_*`, and superseded `RESEARCH_BRIEF_*` for the just-sealed plan moves to `.failsafe/archive/`. Entries listed under "Current Tier 3 occupants" above MUST be updated in the same commit.
4. **Move Tier 4 → Tier 6 at seal** — substantiate-time obligation: the just-shipped plan's `plan-*.md`, `AUDIT_REPORT_<plan>.md`, `IMPLEMENTATION_REPORT_<plan>.md`, and `SUBSTANTIATE_REPORT_<plan>.md` all archive in the same commit as the seal.
5. **Refresh THIS index** — the "Last Reviewed" date at the top of this file MUST advance every time a governance write happens. A stale "Last Reviewed" is itself a drift bug.

## How to add a new governance artifact

1. Decide the tier (use the tier model table above).
2. Add the path to the appropriate tier section in this file.
3. If Tier 1 (Canonical Source), also declare the owner skill and drift signal.
4. If Tier 3 (Active Initiative), declare the close condition.
5. Create the artifact and commit both files together.

## How to retire a governance artifact

1. Confirm the tier-specific archive trigger has fired (substantiate sealed, plan shipped, etc.).
2. Move the file to `.failsafe/archive/<YYYY-MM-<reason>>/<subfolder>/`.
3. Update the tier table in this file to either remove the entry or move it to Tier 6 with provenance.
4. Commit the index update in the same commit as the file move (or in a paired housekeeping commit).

---

## Publication status

Registration in this index and presence in the repository are **different things**, and a reader of a clone cannot tell them apart without this section.

`.gitignore:52-56` heads its stanza *"Private planning docs, transfer files, and governance records (licensing TBD)"* and ignores `docs/` and `.failsafe/` wholesale. Both trees are therefore **private by default and published by exception** — a doc becomes public when someone runs `git add -f` on it, which is a deliberate act. 38 of the 68 paths registered below have had that done; 15 have not.

An unpublished row is not drift. Whether a given governance doc *should* be published is a licensing decision the operator has explicitly deferred, and this index does not presume it.

- private-by-default: `docs/`
- private-by-default: `.failsafe/`

`FailSafe/extension/src/test/governance/governance-index-publication.test.cjs` (FX939) reads those two prefixes from this section — not from a copy of its own — and fails when a registered path is neither tracked nor covered by one. Rows naming a *shape* rather than a file (`plan-*.md`, `AUDIT_REPORT_<plan>.md`, `entry-<N>-body.md`) are skipped; 14 of the 68 are of that kind.

---

## Out-of-tier paths (NOT governance — listed here so they are not confused with governance)

| Path | What it is |
|---|---|
| `src/` | Source code (covered by `FEATURE_INDEX.md` Tier 1) |
| `FailSafe/extension/` | Packaged extension source (covered by `FEATURE_INDEX.md` + `package.json`) |
| `tools/`, `scripts/` | Build / release tooling (covered by `package.json` scripts + `FEATURE_INDEX.md` if user-facing) |
| `node_modules/`, `out/`, `dist/` | Build artifacts (ignored) |
| `PRIVATE/`, `FailSafe-Pro/` | Confidential per `CLAUDE.md` (Pro repo separate) |
| `.claude/`, `.qor/`, `.qorlogic/`, `.agent/`, `.failsafe/` | Tool config + runtime state (governance subsets explicitly tiered above; tool state is NOT governance) |
| `confidentiality.md` | **Operator memory, not a repository file.** Lives in the operator's local Claude memory store and holds pricing and tier internals per `CLAUDE.md`; it is deliberately NOT in this repo and must not be imported. Registered as a root Tier 2 artifact until 2026-09-04, which pointed every reader at a path that has never existed. |

---

**Upstream parallel**: The hierarchical-governance-index pattern itself has been proposed to Qor-logic upstream as a generalizable governance hygiene primitive. If the upstream lands a `GOVERNANCE_INDEX.md` scaffolding template in `/qor-bootstrap`, this file should be reviewed for alignment.
